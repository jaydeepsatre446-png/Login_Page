# Login_Page
 Project is a simple Login Page GUI built using Python and Tkinter. Allows users to enter an email and password to log in. The program checks whether the email is in the correct format and verifies it with a predefined email and password stored in the code. If entered details are correct, success message is displayed; else an error message appears.
